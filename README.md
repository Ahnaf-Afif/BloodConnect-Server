# BloodConnect Server

Backend API for the blood donation platform.

## Run Locally

```bash
npm install
npm run dev
```

The server runs on `http://localhost:5000`.

## Current Routes

- `GET /`
- `GET /health`
